# External repositories

Large or third-party repositories are not vendored here. See `../sources.lock.json` for source URLs and pinned revisions.

The local Wafer checkout is excluded from git because the upstream repository had no declared license when it was reviewed. Jev Ultrafast is MIT-licensed, but the catalog records its upstream and tested revision instead of duplicating its history inside this repository.

